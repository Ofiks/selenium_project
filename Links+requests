'''
Небольшая утилита для сбора ссылок на сайте и ответных кодов ответа сервера. 
Планируется её развитие в скрипт, обходящий все ссылки на сайте с возвращением кода ответа.
'''
from time import sleep
from selenium import webdriver
import requests
from selenium.webdriver.common.by import By

driver = webdriver.Chrome()
driver.get("https://www.qa-practice.com/elements/input/simple")

sleep(5)
elems = driver.find_elements("tag name", "a")
links = []
for el in elems:
    href = el.get_attribute("href")
    if href and href.startswith(("http://", "https://")):
        r = requests.get(href)
        rc =  r.status_code
        print(href, rc)
